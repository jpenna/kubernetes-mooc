apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: ping-loadbalancer-svc
spec:
  type: LoadBalancer # This should be the only unfamiliar part
  selector:
    app: ping
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
