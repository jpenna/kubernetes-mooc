apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: images-svc
spec:
  type: ClusterIP
  selector:
    app: images
  ports:
    - port: 2335
      protocol: TCP
      targetPort: 3000
