apiVersion: v1
kind: Service
metadata:
  name: ping-svc
spec:
  type: LoadBalancer # This should be the only unfamiliar part
  selector:
    app: ping
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
